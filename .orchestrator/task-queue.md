# Task Queue
<!-- Orchestrator reads this to decide next delegation -->

## Queued (in priority order)
1. ECS core: components, systems, world init — feature/ecs-core
2. Spatial grid: hash grid + unit tests — feature/spatial-grid
3. Rendering: Raylib renderer + stats overlay — feature/rendering
4. Integration: wire modules together
5. Normal Boid behavior rules
6. Doctor Boid behavior rules
7. Extensions: debuffs, sex, antivax, UI sliders

## In Progress
None.

## Completed
1. ~~Define shared API headers (include/*.h) — blocks all parallel work~~ (Phase 7)
